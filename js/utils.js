'use strict';

(function () {
  var ENTER_KEY = 'Enter';
  var ESC_KEY = 'Escape';
  var FILE_TYPES = ['gif', 'jpg', 'jpeg', 'png'];

  window.utils = {
    ENTER_KEY: ENTER_KEY,
    ESC_KEY: ESC_KEY,
    FILE_TYPES: FILE_TYPES
  };
})();
